# Starsky 桌面版 · 安装、授权与更新

版本：1.5.3，Windows / macOS 同一源码、同一批插件（11 个）、同一版资产（20260904-r2）。Windows 安装包在 Windows 上实机自检；Mac 包由 GitHub 的 macOS 构建机出品，尚待更多 Mac 实机反馈，遇到问题请按文末方式反馈。

## 先准备

- **授权码**：按年授权、绑定本机。装上后安装向导第一张卡显示本机指纹，点「复制申请信息」发到公众号「跨境者说干货」后台，付费后收到一行 `STARSKY1.` 开头的授权码粘贴进去。没有授权码，向导不会开始安装。
- **DeepSeek API Key**：platform.deepseek.com 注册并创建（`sk-` 开头），按 token 计费，先充几十块够跑一整天。Key 只存在你自己电脑上。
- 能上网：首次会从 npm 镜像下载官方 DSH 底座约 100 MB。
- 不用装 Node、不用装 Python，运行时随包。
- 安装包未做微软 / Apple 代码签名，首次打开会有系统提示（下有说明），属正常现象。

## Windows

1. 到 [Releases](../../../releases/latest) 下载 `Starsky-Setup-<版本>.exe`，双击安装，选位置一路下一步。
2. Windows 弹「已保护你的电脑」时：点 **「更多信息」→「仍要运行」**。
3. 装完自动打开安装向导：第一张卡粘贴授权码；粘贴 DeepSeek Key；「底座方式」保持默认的 **跟官方预览线**；点「开始安装」，等它装完底座、技能、九岗角色、界面插件（首次 3–8 分钟）→ 自动进入工作台。

## macOS

1. 下载与芯片对应的包：Apple 芯片（M1 / M2 / M3 / M4）用 `Starsky-<版本>-mac-arm64.dmg`，Intel 用 `Starsky-<版本>-mac-x64.dmg`。「关于本机」里能看到芯片型号。
2. 打开 dmg，把 Starsky 拖进「应用程序」。
3. 首次打开会提示"无法验证开发者"：**在应用程序里右键 Starsky → 打开 → 再点「打开」**；或在终端执行 `xattr -cr /Applications/Starsky.app`。
4. 之后的流程与 Windows 完全一样：授权码 → DeepSeek Key → 底座方式 → 开始安装。快捷键用 `Cmd` 替代 `Ctrl`，全屏是 `⌃⌘F`。

## 老学员升级

- 已装 1.5.1 / 1.5.2 Starsky：直接安装新包覆盖。
- 已装改名前的「星空亚马逊运营Agent系统」（1.3.1 / 1.5.0）：先在旧版菜单「系统 → 彻底退出（连 AI 服务一起停）」，Windows「设置 → 应用」卸载旧版（**不会**删数据），再装 Starsky。产品改名后 appId 变了，不卸载会并存两个图标。
- 升级后首次打开若停在「授权与续费」页：这台机器还没有授权码，按页面上的三步申请即可。数据目录原地沿用（Windows `%LOCALAPPDATA%\XingkongDsh`，Mac `~/Library/XingkongDsh`），对话、Key、工作区、已装插件全部保留，不用重装底座。新装机数据目录为 `Starsky`。

## 授权

- **一机一码**：授权码内含本机指纹，只在这台机器上有效。指纹来自硬件标识（Windows 注册表 MachineGuid / Mac 主板 UUID），不含个人数据。
- **到期只停更新不停用**：到期后已装的一切照常使用，但不再接收程序更新、技能 / 角色资产更新和底座升级；装带新版资产的新安装包会停在授权页提示续费。续费拿到新码粘贴即可，不用重装。
- **换电脑 / 重装系统**：指纹会变，旧码失效，按新指纹重新申请。
- **在哪看**：菜单「系统 → 授权与续费」（`Ctrl+L` / `Cmd+L`）：状态、到期日、本机指纹、粘贴新码；诊断页也显示授权行。
- 离线验签，程序不联网校验授权，也不上传任何数据。授权码和本机 `license.json` 请勿公开分享。

## 更新

- 程序自更新与资产更新的发布源就是本仓库的 Releases，装完默认已配置。菜单「系统 → 更新中心」（`Ctrl+U`）→「检查更新」。
- 底座（DeepSeek Harness）跟官方走：三源检测，**绝不自动升级**，有新版会问你；升级链自动备份、逐个重新探测界面插件、90 秒启动自检、不通自动回滚。
- 一切更新都需要有效授权；到期后更新中心会提示续费。

## 数据放哪里

| 内容 | Windows | macOS |
| --- | --- | --- |
| 底座、便携 Node、引擎 home、插件、日志、授权 | `%LOCALAPPDATA%\Starsky`（老装机 `XingkongDsh`） | `~/Library/Starsky`（老装机 `~/Library/XingkongDsh`） |
| 你的产品资料与生成报告 | 你在工作台里选的工作区文件夹 | 同 |

卸载不删数据目录，想彻底清理手动删除即可。

## 常见问题

**装完打不开 / 一直转圈？** 菜单「系统 → 诊断修复」看引擎日志；多数是首装网络慢。本机 3182 端口被占用也会卡。

**界面是"裸的"DeepSeek Harness，没有 Starsky 主题和左下角的看板 / 团队 / 步骤？** 首装时插件没探测通过（多为网络中断）。「系统 → 诊断修复 → 修复插件」重跑一次。

**授权码提示"绑定的是另一台机器"？** 码和电脑一一对应，到 `Ctrl+L` 复制本机指纹重新申请。

**Mac 提示"已损坏，无法打开"？** 未签名包被 Gatekeeper 拦下，终端执行 `xattr -cr /Applications/Starsky.app` 后再开。

**「余额 查询失败」红字？** 计费插件查的是 DeepSeek 官方账户；Key 走第三方网关时会失败，不影响使用，「设置 → 费用」可关掉。

## 反馈

公众号 **跨境者说干货** 后台，或本仓库 Issues。请附：Windows / Mac、芯片、Starsky 版本、具体报错；截图先遮住 API Key、授权码和真实店铺信息。
