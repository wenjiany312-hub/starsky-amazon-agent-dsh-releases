# 桌面版更新记录

## 1.5.3 · 2026-09-06 · Windows / macOS 同发

- 首次同时提供 Mac 安装包：`Starsky-1.5.3-mac-arm64.dmg`（Apple 芯片）与 `-x64.dmg`（Intel），由 GitHub 的 macOS 构建机从同一源码出品；自动更新的 `latest-mac.yml` 一并挂出。Mac 包尚待更多实机反馈，问题请按安装说明文末方式反馈。
- 发布仓改名为 `starsky-amazon-agent-dsh-releases`，程序内置更新源与更新中心默认值同步；1.5.2 及之前版本通过 GitHub 的旧地址跳转仍可收到本次更新。
- 授权页与安装向导：公众号二维码直接放进页面，「复制申请信息」一键复制带本机指纹的申请文案，注明一般 24 小时内回复。
- 功能与 1.5.2 一致。

## 1.5.2 · 2026-09-05 · 授权码

- 按年授权、绑定本机、离线验签（Ed25519）。首装无有效授权码不安装；已装机器升级后首次打开停在授权页，换码后自动进入工作台，数据全保留。
- 到期只停更新不停用：程序更新、资产更新、底座升级全部关闭；装带新版资产的新包会提示续费；已装内容照常使用。
- 菜单「系统 → 授权与续费」（`Ctrl+L`）：状态、到期日、本机指纹、粘贴新码。
- 程序默认更新源指向公开发布仓。

## 1.5.1 · 2026-09-04 · 更名 Starsky，并入 Mac 修订

- 产品统一更名为 **Starsky**（原「星空亚马逊运营Agent系统」）：安装包、程序、快捷方式、卸载项、界面文字、插件显示名、资产包全部换新名；appId 改为 `com.starsky.desktop`，老学员需先卸载旧版再装；新装机数据目录 `Starsky`，老装机原地沿用 `XingkongDsh`。
- 并入 9 月 3 日 Mac 实机修订中平台无关的修复：桥接插件前私装 pnpm（否则学员机 11 个界面插件全部装不上）、桥接器支持 glob / 清理半截快照 / 同插件升版自动回滚、不再误杀无关 node 进程、交接消息口语化也能识别「⚡ 接力 → 下一岗」、所有窗口加右键菜单。
- Windows 与 Mac 源码合并为同一仓库，学员包资产入仓（20260904-r2）。

## 1.5.0 · 2026-09-03

- 底座改为跟随官方 0.1.2-alpha 线，更新中心三源检测，确认后升级、探测不过自动回滚。
- 11 个界面插件按新底座 API 重新移植，新增 Univer Office（Excel / Word / PPT）与 AnySearch 联网搜索。
- 一批稳态修复：新建会话失败、启动慢、粘图识图、接力按钮、首装插件探测预算等。
